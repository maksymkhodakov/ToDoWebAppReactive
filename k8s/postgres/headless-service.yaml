apiVersion: v1
kind: Service
metadata:
  name: postgres-headless
  namespace: todo-app
spec:
  clusterIP: None          # headless: required by StatefulSet for stable pod DNS
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
